server {
    listen 80;
    listen [::]:80;
    server_name work.pelmon.kro.kr;
    location ^~ /.well-known/acme-challenge/ {
        root /volume1/docker/work-automation/acme;
    }
    location / { return 503; }
}
